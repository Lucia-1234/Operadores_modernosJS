
// funcion que valida objetos. Param: el item que va a ser validado
export const esObjeto = (item) => item !== null && typeof item === 'object' && !Array.isArray(item);


// Esquema de jugadores (Usado hasta ahora unicamente en el punto 4)
